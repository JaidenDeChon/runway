# Source

Vendored from https://github.com/blader/humanizer at commit `9862685f575c65a8247f90369951df1b3416e3d6` (2026-09-06), skill version 3.0.0. MIT licensed; see `LICENSE`.

`SKILL.md` is an unmodified copy. Only `SKILL.md` and `LICENSE` are vendored; the upstream repository's README, plugin manifests and validation script are left out. The `humanizer` entry in `skills-lock.json` records the upstream folder's hash at that commit, as `bunx skills add blader/humanizer` writes it, so the skills CLI sees this copy as current.

To update, copy the new `SKILL.md` over it, change the commit above, and update the lock entry's `computedHash`. Runway-specific rules for applying it live in `.claude/agents/copy-humanizer.md`, not here.
